# Getting Started

> **Status:** outline — prose to be written.

<!-- NOTE: one-paragraph overview of the path from clone → built binary →
     first command. Then the three cards below route to the steps. The
     fast-path is `./setup.sh`; the longer path is download_data + convert +
     make. -->

<div class="grid cards" markdown>

-   :material-download: **[Installation & Build](installation.md)**

    Clone, `./setup.sh`, and the `make` targets / build modes.

-   :material-database: **[Data Setup](data-setup.md)**

    Lexica, layouts, letter distributions, and strategy tables.

-   :material-rocket-launch: **[Quick Start](quick-start.md)**

    Your first session in the REPL.

</div>

## Prerequisites

<!-- NOTE: compiler (C99/C11 subset), make, platform notes (macOS/Linux),
     python3 for the helper scripts, git. Mention disk needs for data/. -->

## The fast path

<!-- NOTE: `git clone` → `cd MAGPIE` → `./setup.sh` → `./bin/magpie`.
     setup.sh wraps download_data.sh + convert_lexica.sh + a build. -->
