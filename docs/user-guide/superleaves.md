# Superleave Generation

> **Status:** outline — prose to be written.
> **Sources:** `src/impl/rack_list.c`, `src/impl/rack_info_table_maker.c`
> **Deep dive:** [Leaves & Equity](../algorithms/leaves-and-equity.md)

## What a leave is

<!-- NOTE: the tiles kept after a play; their value is added to score to form
     static equity. "Superleaves" = the learned leave-value table (KLV). -->

## Generating leave values

<!-- NOTE: the workflow/command to produce a KLV from sampled racks and game
     data. Inputs, outputs (.klv), and how it ties into autoplay. -->

## Sampling vs. exhaustive

<!-- NOTE: why leaves are sampled rather than enumerated for large lexicons;
     the RIT (rack info table). Link to the algorithms page for detail. -->

## Using generated leaves

<!-- NOTE: `-leaves <name>` in simulation/autoplay. -->
