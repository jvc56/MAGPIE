# Command Reference

> **Status:** outline — prose to be written.
> **Source of truth:** the in-app `help` output; `src/impl/config.c`, `src/impl/exec.c`

<!-- NOTE: an exhaustive table of every command — name, one-char shortcut,
     positional args, and a one-line description. Mirror `help`. Consider
     grouping (game, analysis, data/convert, async). A short script could
     generate this from `help` output; for now, fill the table by hand.

     Suggested columns: Command | Shortcut | Arguments | Description -->

## Game & annotation

<!-- NOTE: new, p1/p2, switchnames, r (rack), c (commit), t (tcommit),
     challenge/unchallenge, goto, load, export, ... -->

## Move generation & analysis

<!-- NOTE: generate (g), sim, gsim, rgs/rgsimulate, infer, endgame, peg, ... -->

## Data & conversion

<!-- NOTE: convert (text2kwg, text2wordmap), cgp, set, ... -->

## Batch

<!-- NOTE: autoplay, ... -->

## Async (interactive)

<!-- NOTE: status (sta), stop (sto). -->
